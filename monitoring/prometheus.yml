global:
  scrape_interval: 15s

scrape_configs:
  - job_name: triprank-api
    static_configs:
      # Local-dev mode: API runs on host uvicorn, not as a compose service.
      # Flip back to ["api:8000"] only when API runs as a compose service.
      - targets: ["host.docker.internal:8000"]
